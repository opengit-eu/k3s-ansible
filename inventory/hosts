---
opencommit:
  hosts:
    dev1:
      ansible_host: "{{ lookup('env', 'KATES1_IP') }}"
    dev2:
      ansible_host: "{{ lookup('env', 'KATES2_IP') }}"
    dev3:
      ansible_host: "{{ lookup('env', 'KATES3_IP') }}"
  vars:
    ansible_port: 2849