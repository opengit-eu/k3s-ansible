---
opencommit:
  hosts:
    dev1:
      ansible_host: "{{ lookup('env', 'PROD1_IP') }}"
    dev2:
      ansible_host: "{{ lookup('env', 'PROD2_IP') }}"
    dev3:
      ansible_host: "{{ lookup('env', 'PROD3_IP') }}"
  vars:
    ansible_port: 2849